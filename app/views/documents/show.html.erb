<main>
  <p><%= link_to "Back to documents", documents_path %></p>

  <h1><%= @document.title %></h1>

  <% if @document.description.present? %>
    <p><%= @document.description %></p>
  <% end %>

  <dl>
    <dt>Status</dt>
    <dd><%= @document.status.humanize %></dd>

    <dt>Original filename</dt>
    <dd><%= @document.original_filename %></dd>

    <dt>Content type</dt>
    <dd><%= @document.content_type %></dd>

    <dt>Size</dt>
    <dd><%= number_to_human_size(@document.byte_size) if @document.byte_size %></dd>
  </dl>

  <% if @document.file.attached? %>
    <p><%= link_to "Download file", rails_blob_path(@document.file, disposition: "attachment") %></p>
  <% end %>

  <% if @document.document_chunks.any? %>
    <section>
      <h2>Ingestion</h2>
      <dl>
        <dt>Chunks</dt>
        <dd><%= @document.document_chunks.count %></dd>

        <dt>Embeddings</dt>
        <dd><%= @document.document_embeddings.count %></dd>
      </dl>

      <h3>Chunks</h3>
      <% @document.document_chunks.includes(:document_page).each do |chunk| %>
        <article>
          <h4><%= chunk.label.humanize %> chunk <%= chunk.chunk_index %></h4>
          <p>Starts on page <%= chunk.document_page.page_number %></p>
          <pre><%= chunk.content %></pre>
        </article>
      <% end %>
    </section>
  <% end %>

  <%= button_to "Delete document", @document, method: :delete, form: { data: { turbo_confirm: "Delete this document?" } } %>
</main>
